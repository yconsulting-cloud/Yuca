import ShopshotsClient from './ShopshotsClient';

export const metadata = {
  title: 'Yuca — Shopshots',
  description: 'Shopshots — générateur de photos produit',
};

export default function ShopshotsPage() {
  return <ShopshotsClient />;
}
